dependencies {
    compile project(':javabio-algorithms')
    compile libs.apache_math3
    compile(libs.biojava) {
        exclude group: 'org.apache.logging.log4j'
    }
    compile libs.biojava_1

    testCompile libs.test
}